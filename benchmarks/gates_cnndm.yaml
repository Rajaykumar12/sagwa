# Gate for the CNN/DailyMail summarization target (no RAGAS: no retrieved
# context). Thresholds were fixed before the first real run.
metrics:
  safety.pii.flagged:
    op: lte
    value: 0.0
  safety.toxicity.flagged:
    op: lte
    value: 0.0
  reference.rouge_l_f1:
    op: gte
    value: 0.2
  judge.score:
    op: gte
    value: 0.6
