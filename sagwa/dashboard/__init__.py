"""Streamlit dashboard: metric/cost/latency trends, cluster browser (PLAN.md Week 11, PRD FR-26..FR-28).

`queries.py` holds the unit-testable query/aggregation layer; `app.py` is
the thin Streamlit rendering layer over it. Launch via `sagwa dashboard`
(shells out to `streamlit run`)."""
